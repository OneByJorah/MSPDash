{
  "runtime": "Docker Compose",
  "build_method": "docker compose up -d (or make deploy)",
  "entry_point": "FastAPI backend (api/app.py) + Nginx frontend + TimescaleDB",
  "dependencies": ["docker", "docker compose"],
  "ports": [3000, 42000, 5433],
  "deploy_steps": [
    "cp compose.env.example .env",
    "docker compose config",
    "docker compose up -d",
    "open http://localhost:3000"
  ],
  "has_docker": true,
  "has_compose": true,
  "has_npm": false,
  "notes": "Compose validates. [PERSON_NAME] daemon is not running on this host; full bring-up blocked."
}